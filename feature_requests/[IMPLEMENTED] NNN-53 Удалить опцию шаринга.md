[IMPLEMENTED] - Реализовано 2025-12-03

Функциональность шаринга в нынешнем виде вызывает больше вопросов, чем приносит пользы, поэтому имеет смысл её убрать.

Для этого следует:

1. Убрать из UX поддержку read-only режима
2. Убрать из БД функции и политики, обеспечивающие шаринг

Реализовано:
- Удалена логика шеринга из ProductContext (sharedUserId, isReadOnly, обработка share параметра)
- Удален UI шеринга из SettingsDialog (переключатель, share link, кнопка копирования)
- Убраны проверки isReadOnly и sharedUserId из всех страниц (Index, Strategy, Roadmap, Board, Hypotheses)
- Создана миграция БД для удаления колонок (is_public, share_token), функций (get_shared_user_id, is_project_public) и RLS политик
- Обновлена документация (удалены упоминания шеринга)
- Обновлены типы TypeScript